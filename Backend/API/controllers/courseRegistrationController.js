const User = require("../models/courseRegistrationModel.js");

const get = async (request, response) => {
 
};

const edit = async (request, response) => {
  
};

const del = (request, response) => {
  
};

module.exports = {get, edit, del};
